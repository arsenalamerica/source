export type VenueData = {
  data: {
    id: number;
    name: string;
    surface: string;
    address: string;
    city: string;
    capacity: number;
    image_path: string;
    coordinates: string;
  };
};
