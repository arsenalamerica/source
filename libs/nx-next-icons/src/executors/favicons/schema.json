{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Favicons executor",
  "description": "",
  "type": "object",
  "properties": {
    "iconOutputDir": {
      "type": "string",
      "description": "Directory to output generated icons to."
    },
    "svgLogoPath": {
      "type": "string",
      "description": "Path to the SVG file to use for generating icons."
    }
  },
  "required": []
}
